1、CConfigFileReader 类，方便的从任何文件中读取配置项
2、配置项组织形式为 KEY = VALUE 等号前后可以有空格
3、注释以#开头，可以再文件开始位置也可以在每行的末尾，

内部实现为一个MAP来保存所有的KEY-VALUE
test.cpp为一个简单的测试代码文件，config.conf为一个配置文件，保存了几个配置项，
使用者可以创建一个VC工程，将以上几个文件加入到工程，可以直接运行，查看测试结果